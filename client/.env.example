# Développement local (Vite séparé du serveur)
VITE_SERVER_URL=http://localhost:3001

# Production Render (tout-en-un) : laisser VITE_SERVER_URL vide —
# le client utilise automatiquement window.location.origin

# Optionnel — synthèse vocale ElevenLabs
# VITE_ELEVENLABS_API_KEY=
# VITE_ELEVENLABS_VOICE_ID=
